name: Cypress Tests

on: 
  push:
   paths:
      - 'UInnovateApp/**'
      - .github/workflows/validate-pr.yml

jobs:
  cypress-run:
    runs-on: ubuntu-latest
  steps:
      - uses: actions/checkout@v3

      - name: Cypress tests 🧪
        uses: cypress-io/github-action@v5
        with:
          project: UInnovateApp